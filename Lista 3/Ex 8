using System;

class Program {
  static void Main(string[] args) {
      
    //Declarando as variaveis
    int L, a = 0, b = 1;; 
    
    Console.Write("Digite um número inteiro positivo L: ");
    L = int.Parse(Console.ReadLine());
    
    //Início do laço de repetição
    for (int i = 0; i < L; i++)
    {
      Console.Write(a + " ");
      int c = a + b;
      a = b;
      b = c;
    }
  }
}
